#!/usr/bin/env ruby
# frozen_string_literal: true
# Run all workspace tests or selected test targets.

require_relative "../lib/workspace/services/test"

exit Workspace::Services::Test.new(ARGV).call
