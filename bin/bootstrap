#!/usr/bin/env ruby
# frozen_string_literal: true
# Bootstrap workspace repositories, dependencies, and database setup.

require_relative "../lib/workspace/services/bootstrap"

exit Workspace::Services::Bootstrap.new.call
