#!/usr/bin/env ruby
# frozen_string_literal: true
# Print branch and working-tree status across workspace repositories.

require_relative "../lib/workspace/services/status"

exit Workspace::Services::Status.new.call
