#!/usr/bin/env ruby
# frozen_string_literal: true
# Verify local toolchain, auth, Docker daemon, and expected ports.

require_relative "../lib/workspace/services/doctor"

exit Workspace::Services::Doctor.new.call
