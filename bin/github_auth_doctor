#!/usr/bin/env ruby
# frozen_string_literal: true
# Run GitHub authentication and permission diagnostics for repo provisioning workflows.

require_relative "../lib/workspace/services/auth/github_auth"

exit Workspace::Services::Auth::GithubAuth.new.call
