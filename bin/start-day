#!/usr/bin/env ruby
# frozen_string_literal: true
# Run the standard morning workflow and optionally start development services.

require_relative "../lib/workspace/services/start_day"

exit Workspace::Services::StartDay.new(ARGV).call
