#!/usr/bin/env ruby
# frozen_string_literal: true
# Launch primary local development services across workspace repositories.

require_relative "../lib/workspace/services/dev"

exit Workspace::Services::Dev.new.call
