#!/usr/bin/env ruby
# frozen_string_literal: true
# Fast-forward pull all configured git repositories in the workspace.

require_relative "../lib/workspace/services/pull"

exit Workspace::Services::Pull.new.call
