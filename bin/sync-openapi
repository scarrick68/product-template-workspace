#!/usr/bin/env ruby
# frozen_string_literal: true
# Sync API OpenAPI spec into shared contract and consumer locations.

require_relative "../lib/workspace/services/sync_openapi"

exit Workspace::Services::SyncOpenapi.new.call
