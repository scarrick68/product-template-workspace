#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/workspace/services/init_project"

exit Workspace::Services::InitProject.new(ARGV).call#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/workspace/services/init_project"

exit Workspace::Services::InitProject.new(ARGV).call